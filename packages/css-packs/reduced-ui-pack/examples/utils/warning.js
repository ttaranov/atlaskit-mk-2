// @flow
import React from 'react';

export default () => (
  <p style={{ paddingBottom: '8px' }}>
    <b>NOTE:</b> As this example is written in react, you will need to change{' '}
    <code>className</code> to <code>class</code> to render them in base html.
  </p>
);
