describe('Uploader', () => {
  it('should pass down the file content to Chunkinator', () => {});
  it('should create a MediaStore with the given config', () => {});
  it('should use Rusha for the hash creation', () => {});
  it('should use the file and collection names when creating the file', () => {});
  it('should call onProgress with the upload percentage', () => {});
  it('should resolve with the fileId when the upload has finished', () => {});
  it('should reject if there was an error with the upload', () => {});
});
