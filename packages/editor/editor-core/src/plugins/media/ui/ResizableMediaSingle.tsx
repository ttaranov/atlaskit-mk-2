import * as React from 'react';
import { MediaSingleProps } from '@atlaskit/editor-common';

export default function ResizableMediaSingle({
  children,
  layout,
  width,
  height,
  containerWidth = width,
  isLoading = false,
  className,
}: MediaSingleProps) {
  return <p>Hello</p>;
}
