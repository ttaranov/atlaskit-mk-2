import * as React from 'react';

import { tallImage as imageDataUri } from '@atlaskit/media-test-helpers';
import { NextGenFullEditor } from '../example-helpers/nextGenFullEditor';

export default () => <NextGenFullEditor imageUrl={imageDataUri} />;
