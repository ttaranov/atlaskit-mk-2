// @flow

// All entrypoints should start with `./dist/cjs/` instead of `./src` folder since they're connected to the bundle
import LayoutManagerWithViewController from './dist/cjs/components/connected/LayoutManagerWithViewController';

export default LayoutManagerWithViewController;
