import {
  customCategory as custom,
  defaultEmojiHeight as height,
  frequentCategory as frequent,
  defaultCategories as categories,
  analyticsEmojiPrefix as analytics,
} from '@atlaskit/emoji';

export const customCategory = custom;
export const defaultEmojiHeight = height;

export const frequentCategory = frequent;
export const customTitle = 'All uploads';
export const userCustomTitle = 'Your uploads';

export const emojiPickerWidth = 350;
export const emojiPickerHeight = 295;

export const defaultCategories = categories;

export const analyticsEmojiPrefix = analytics;
