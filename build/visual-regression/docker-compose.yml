version: '3'

services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /opt/atlaskit-mk2
    container_name: "dev"
    volumes:
      - ../../../atlaskit-mk2:/opt/atlaskit-mk2
    entrypoint:
      - ls 