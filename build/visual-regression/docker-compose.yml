version: '3'

services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile.visual-regression
    working_dir: /opt/atlaskit-mk2
    container_name: "dev"
    volumes:
      - "../../../atlaskit-mk2:/opt/atlaskit-mk2"
      - /opt/atlaskit-mk2/node_modules