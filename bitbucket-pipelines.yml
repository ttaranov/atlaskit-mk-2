image: node:8.4.0
pipelines:
  branches:
    master:
      - step:
          script:
            - source ./build-setup.sh
            - bolt install
            - yarn build
            - yarn lint
            - yarn test
            - yarn release

  default:
    - step:
        script:
          - source ./build-setup.sh
          - bolt install
          - yarn build
          - yarn lint
          - yarn test
