{
  "presets": [["env", { "modules": false }], "react", "flow"],
  "plugins": [
    // transform-runtime causes issues in badge's jest tests
    // "transform-runtime",
    "transform-class-properties",
    "transform-object-rest-spread" // used in build, probably want to disable in components soon
  ]
}
